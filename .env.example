# Database Configuration
DATABASE_URL=postgresql://cip_user:cip_password@postgres:5432/conflict_intelligence

# Redis Configuration
REDIS_URL=redis://redis:6379/0

# API Configuration
# Value used by the browser/client (must be reachable from your host)
NEXT_PUBLIC_API_URL=http://localhost:8000
# Internal URL used when the frontend server runs inside Docker
API_INTERNAL_URL=http://backend:8000

# CORS — comma-separated origins allowed during development
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Optional: Add API keys if needed for advanced features
# OPENAI_API_KEY=your_openai_key_here
# ANTHROPIC_API_KEY=your_anthropic_key_here
